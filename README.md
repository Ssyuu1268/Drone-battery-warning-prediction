# drone-battery-warning-prediction
Predict drone battery warning levels from sensor data via correlation-based feature selection and discretization, then model with a Bayesian Network (Hill Climb + k2score, BDeu). Benchmarked against Random Forest and XGBoost, addressing class imbalance with weighting and SMOTE.
